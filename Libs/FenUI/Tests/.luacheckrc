-- Luacheck config for Tests
-- Adds Busted globals

globals = {
    "describe", "it", "before_each", "after_each", "setup", "teardown",
    "assert", "spy", "stub", "mock", "pending", "RunTests",
}

